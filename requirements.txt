# ==============================================================================
# Vergeclip AI — Production Requirements (MAIN APP)
# ------------------------------------------------------------------------------
# `pip install -r requirements.txt` installs EVERYTHING the web app needs to run
# (FastAPI, OpenCV, Pillow, yt-dlp, SQLAlchemy, JWT, faster-whisper, edge-tts,
# huggingface_hub, psutil, providers…). Verified fresh-install compatible.
#
# ⚠️  LOCAL AI VIDEO GENERATION (Wan2.1 / LTX-Video via ComfyUI) uses a SEPARATE
#     runtime: `requirements-local.txt` → installed into `comfyui/.venv`
#     via Admin UI (🔧 Setup & Start button) or:
#         .\scripts\setup_comfyui_wan.ps1
#     Only needed for fully-offline GPU scene/clip generation.
# ==============================================================================

# ── Core & Configuration ──────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.28.0
tqdm>=4.66.0
colorama>=0.4.6
psutil>=5.9.0                # /api/admin/system-health CPU/RAM/disk metrics

# ── Web Server & API Framework ────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9       # required by FastAPI for multipart/form parsing
email-validator>=2.1.0        # required by Pydantic EmailStr validation

# ── AI / LLM Providers ─────────────────────────────────────────────────────────
# Used by app/semantic_ranker.py (routed via HTTP — no model files needed)
google-generativeai>=0.8.0    # Gemini: set GOOGLE_API_KEY + RANKING_PROVIDER=gemini
openai>=1.14.0                # OpenAI / Groq / OpenRouter / DeepSeek (OpenAI-compatible)
assemblyai>=0.20.0            # AssemblyAI cloud transcription
huggingface_hub>=0.24.0       # Auto-download of HF models; used by comfyui/setup endpoint

# ── Video Processing & Computer Vision ────────────────────────────────────────
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.26.0
yt-dlp>=2024.3.10
imageio-ffmpeg>=0.4.9         # bundles an FFmpeg binary as PATH fallback

# ── Transcription & Audio ─────────────────────────────────────────────────────
faster-whisper>=1.0.0         # local STT provider (app/transcriber.py); wrapped in try/except
edge-tts>=6.1.0               # TTS engine for script-to-video narration

# ── Database & Persistence (SQLite default; Postgres/async optional) ───────────
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0

# ── Authentication & Security ─────────────────────────────────────────────────
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
