# ==============================================================================
# Vergeclip AI — LOCAL VIDEO GENERATION requirements (CogVideoX-2B via ComfyUI)
# ------------------------------------------------------------------------------
# ⚠️ INSTALL INTO COMFYUI'S OWN VIRTUAL ENV (comfyui/.venv), NOT the app venv.
#    This manifest is ONLY needed for fully-OFFLINE, GPU-based AI scene/clip
#    generation. The main web app does NOT require any of these packages.
#
# Verified working with:
#   - NVIDIA GPU (RTX 3050 / 6GB VRAM tested; any NVIDIA GPU with >=4GB VRAM)
#   - Python 3.10 – 3.12 (ComfyUI requirement; 3.13 is NOT supported)
#   - PyTorch 2.7.1+cu126 / CUDA 12.6
#
# Equivalent single install command (auto-run by scripts/setup_comfyui_cogvideo.ps1):
#   .venv\Scripts\python -m pip install -r requirements-local.txt
# ==============================================================================

# ── PyTorch (CUDA 12.6 build — matches proven RTX 3050 install) ───────────────
# The +cu126 local versions resolve via the extra-index-url below.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.7.1+cu126
torchvision==0.22.1+cu126
torchaudio==2.7.1+cu126

# ── ComfyUI CogVideoXWrapper custom node requirements ────────────────────────
# https://github.com/kijai/ComfyUI-CogVideoXWrapper
huggingface_hub>=0.30.0
diffusers>=0.33.1
accelerate>=0.33.0
einops>=0.8.0
peft>=0.13.0
opencv-python>=4.9.0

# ── CogVideoX model / inference support ──────────────────────────────────────
transformers>=4.45.0
safetensors>=0.4.0
sentencepiece>=0.2.0
tokenizers>=0.20.0
torchsde>=0.2.6         # required by CogVideo pipelines for SDE sampling
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
av>=13.1.0

# ── T5 text encoder (fp8) helper libs ────────────────────────────────────────
accelerate>=0.33.0

# ══════════════════════════════════════════════════════════════════════════════
# MODEL FILES (downloaded into comfyui/models/ by the setup script):
#   - CogVideoX-2b transformer:                                 comfyui/models/CogVideo/.../diffusion_pytorch_model.safetensors
#   - T5 fp8 text encoder:          comfyui/models/text_encoders/t5xxl_fp8_e4m3fn.safetensors
# ══════════════════════════════════════════════════════════════════════════════
